# 3D models (local)

Place Mixamo / Blender exports here. These files are gitignored because they are large.

Expected file for the lab:

- `penalty-kick.glb` — Mixamo Ch28 character with `mixamo.com` kick clip

Copy from Downloads:

```bash
cp "/Users/nathanalbe/Downloads/Soccer Penalty Kick (1).fbx.glb" public/models/penalty-kick.glb
```
